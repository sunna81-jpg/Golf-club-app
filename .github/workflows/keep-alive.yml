name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 3 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s -o /dev/null -w "status: %{http_code}\n" \
            "https://xhatuhygnostnlbczunh.supabase.co/rest/v1/app_storage?select=key&limit=1" \
            -H "apikey: sb_publishable_K4iG7R8BvfCqThJooMjIoA_dIGBzYfG"
